server:
  port: 8081

spring:
  application:
    name: salon-service

aws:
  dynamodb:
    endpoint: https://dynamodb.us-east-1.amazonaws.com
    region: us-east-1
  # Credentials should be fetched via IAM Roles or AWS Secrets Manager (Skill: aws-secrets-manager)
